import { HLComponent } from "framework";

class AppHeader extends HLComponent{
    constructor(config){
        super(config);
    }
}

export const appHeader = new AppHeader({
    selector: 'app-header',
    template: /*html*/``,
      styles: /*css*/``
});